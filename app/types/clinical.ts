export interface Medication {
    name: string;
    dosage: string;
    frequency: string;
    route: string;
} 

export interface Allergy {
    name: string;
    severity: string;
    reaction: string;
}